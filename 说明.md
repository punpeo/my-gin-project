
文件名称接口：

GET /api/v1/files/names → handler.GetFilesNamesHandler

库存统计接口：

POST /api/v1/stock/statistic → handler.StockStatisticHandler

POST /api/v1/stock/delete-date-column → handler.DeleteDateColumnHandler

GET /api/v1/stock/download → handler.StockFileDownloadHandler（新增库存文件下载）

POST /api/v1/stock/cleanup → handler.CleanupFileHandler（新增清理文件）

订单汇总接口：

GET /api/v1/order/download-template → handler.DownloadOrderTemplate（下载模板）

POST /api/v1/order/upload-process → handler.UploadAndProcessOrderExcel（上传并处理订单Excel）

店铺订单统计接口：

POST /api/v1/shop-order/statistic → handler.StatShopOrder（新增）

产品周期销量统计接口：

POST /api/v1/cycle-sales/statistic → handler.CycleSalesStatisticHandler（新增）

Excel处理接口：

POST /api/v1/excel/process → handler.NewExcelHandler().ProcessExcel（修正：函数式挂载）

POST /api/v1/excel/fill → excelFillHandler.FillExcel（新增：方法式挂载，使用NewExcelFillHandler创建实例）